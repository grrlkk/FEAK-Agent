# FEAK-Agent core/dev dependencies.
#
# For Kanana-based MVP runtime, install requirements-kanana.txt instead.
# For the legacy UKTA/KoBERT scorer runtime, install requirements-legacy.txt.

pydantic>=2.0
pymongo>=4.6
openai>=1.0
PyYAML>=6.0
pytest>=8.0
python-dotenv
